import { Link } from "react-router-dom";

export default function SearchPage() {
    return (
        <div>
            검색 페이지
            <Link to="/LoginPage">로그인페이지가기</Link>
        </div>
    );
}
